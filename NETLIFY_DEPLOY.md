# 🚀 Деплой Frontend на Netlify

## Быстрая инструкция

### 1. Подготовка репозитория

Убедитесь, что все файлы закоммичены и запушены в Git:

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

### 2. Создание сайта на Netlify

1. Зайдите на [app.netlify.com](https://app.netlify.com)
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите ваш Git провайдер (GitHub/GitLab/Bitbucket)
4. Выберите репозиторий PlayFlon

### 3. Настройка сборки

В настройках сайта укажите:

- **Base directory:** `frontend`
- **Build command:** `npm install && npm run build`
- **Publish directory:** `.next`

Или используйте автоматическое определение через `netlify.toml` (уже настроен).

### 4. Переменные окружения

В **Site settings → Environment variables** добавьте:

```
NEXT_PUBLIC_API_URL = http://your-backend-url:3000
```

**Важно:** Firebase credentials уже настроены в коде, но если нужно переопределить, добавьте:
- `NEXT_PUBLIC_FIREBASE_API_KEY`
- `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`
- и т.д.

### 5. Деплой

Нажмите **"Deploy site"**. Netlify автоматически:
- Установит зависимости
- Соберёт проект
- Задеплоит сайт

### 6. Настройка домена

1. В Netlify Dashboard: **Site settings → Domain management**
2. Нажмите **"Add custom domain"**
3. Введите `playflon.com`
4. Следуйте инструкциям по настройке DNS:
   - Добавьте A-запись или CNAME согласно инструкциям Netlify
   - Подождите распространения DNS (до 48 часов)

### 7. SSL сертификат

Netlify автоматически выдаст бесплатный SSL сертификат через Let's Encrypt.

### 8. Автоматический деплой

При каждом push в основную ветку репозитория Netlify автоматически:
- Запустит сборку
- Задеплоит новую версию
- Отправит уведомление (если настроено)

## Проверка работы

После деплоя проверьте:

1. ✅ Сайт открывается: `https://your-site.netlify.app`
2. ✅ Авторизация работает (кнопка "Войти через Google")
3. ✅ Выбор настроения работает
4. ✅ Подключение к backend API работает (проверьте Network tab в DevTools)

## Troubleshooting

### Ошибка сборки

- Проверьте логи в Netlify Dashboard → Deploys → [deploy] → Build log
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте версию Node.js (должна быть 20+)

### Ошибка подключения к API

- Проверьте переменную `NEXT_PUBLIC_API_URL` в Netlify
- Убедитесь, что backend доступен и CORS настроен правильно
- Проверьте Network tab в браузере на наличие ошибок CORS

### Firebase Auth не работает

- Проверьте, что домен добавлен в Firebase Console:
  - Authentication → Settings → Authorized domains
  - Добавьте ваш Netlify домен (например, `your-site.netlify.app`)
  - Добавьте кастомный домен `playflon.com`

### Next.js плагин не работает

Убедитесь, что установлен плагин `@netlify/plugin-nextjs`:
- В Netlify Dashboard: **Site settings → Build & deploy → Plugins**
- Плагин должен быть автоматически установлен через `netlify.toml`

## Полезные ссылки

- [Netlify Next.js документация](https://docs.netlify.com/integrations/frameworks/nextjs/)
- [Netlify Environment Variables](https://docs.netlify.com/environment-variables/overview/)
- [Netlify Custom Domains](https://docs.netlify.com/domains-https/custom-domains/)









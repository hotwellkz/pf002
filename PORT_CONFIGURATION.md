# 🔧 Конфигурация портов

## Текущая настройка

- **Backend:** Порт `3000` (настроено в `backend/.env` как `PORT=3000`)
- **Frontend:** Порт `3000` для Next.js dev server (по умолчанию)
- **API URL:** `http://localhost:3000` (frontend обращается к backend)

## Изменение порта backend

Если нужно изменить порт backend:

1. Отредактируйте `backend/.env`:
```env
PORT=3001
```

2. Обновите `frontend/.env.local`:
```env
NEXT_PUBLIC_API_URL=http://localhost:3001
```

3. Перезапустите оба сервера:
```bash
# Backend
cd backend
npm run dev

# Frontend (в другом терминале)
cd frontend
npm run dev
```

## Проверка работы

После настройки проверьте:

1. Backend health check:
```bash
curl http://localhost:3000/health
```

Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. Frontend должен открываться на: `http://localhost:3000` (Next.js)

3. В браузере DevTools → Network проверьте, что запросы идут на правильный порт.

## Важно

- Backend и Frontend могут работать на одном порту (3000), так как Next.js использует свой порт для dev server
- В production backend будет на отдельном сервере/порту
- Для Netlify frontend будет на порту 80/443 (HTTPS)









# 🔧 Исправление проблемы с портом

## Проблема: "EADDRINUSE: address already in use :::3000"

Порт 3000 уже занят другим процессом.

## Решение 1: Использовать другой порт (рекомендуется)

Я изменил порт по умолчанию на **3001** в коде.

Теперь backend будет запускаться на порту 3001.

**Важно:** Обновите frontend `.env.local`:
```env
NEXT_PUBLIC_API_URL=http://localhost:3001
```

## Решение 2: Освободить порт 3000

Если хотите использовать порт 3000:

1. Найдите процесс, занимающий порт:
```bash
netstat -ano | findstr :3000
```

2. Остановите процесс:
```bash
taskkill /F /PID <PID>
```

3. Или измените порт в `backend/.env`:
```env
PORT=3000
```

## Решение 3: Использовать переменную окружения

В `backend/.env` укажите другой порт:
```env
PORT=3001
```

Или любой другой свободный порт.

## Проверка

После исправления запустите:
```bash
npm run dev
```

Должно появиться:
```
✅ Firebase Admin инициализирован
🚀 Playflon Backend запущен на порту 3001
```

Проверьте работу:
```bash
curl http://localhost:3001/health
```










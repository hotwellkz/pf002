# 🔧 Исправление подключения к API

## Проблема: 404 при обращении к backend

Ошибка возникает, когда `NEXT_PUBLIC_API_URL` не установлена или неправильно настроена.

## Решение

### 1. Создайте .env.local файл

В папке `frontend/` создайте файл `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3001
```

### 2. Перезапустите Next.js dev server

После создания `.env.local` нужно перезапустить dev server:

```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова:
npm run dev
```

**Важно:** Next.js загружает переменные окружения только при старте, поэтому после изменения `.env.local` нужен перезапуск.

### 3. Проверьте, что backend запущен

Убедитесь, что backend работает:

```bash
cd backend
npm run dev
```

Должно быть:
```
✅ Firebase Admin инициализирован
🚀 Playflon Backend запущен на порту 3001
```

Проверьте health endpoint:
```bash
curl http://localhost:3001/health
```

### 4. Проверьте в браузере

Откройте DevTools (F12) → Console и проверьте:
- Нет ли ошибок CORS
- Правильный ли URL используется в запросах

## Код уже обновлён

Я обновил код, чтобы использовать fallback на `http://localhost:3001`, если переменная не установлена. Но лучше создать `.env.local` для явной настройки.

## Для production (Netlify)

В Netlify Dashboard добавьте переменную окружения:
- **Key:** `NEXT_PUBLIC_API_URL`
- **Value:** `http://your-backend-url:3001` (или ваш production URL)








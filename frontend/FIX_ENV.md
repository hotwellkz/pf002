# 🔧 Исправление .env.local

## Проблема

В `.env.local` указан порт 3001, но backend запущен на порту 3000.

## Решение

Обновите файл `frontend/.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3000
```

## Важно

После изменения `.env.local` **ОБЯЗАТЕЛЬНО перезапустите Next.js dev server**:

1. Остановите текущий процесс (Ctrl+C)
2. Запустите снова:
```bash
cd frontend
npm run dev
```

Next.js загружает переменные окружения только при старте!

## Проверка

После перезапуска проверьте в браузере DevTools (F12) → Console:
- Должен использоваться URL: `http://localhost:3000/api/session/start`
- Не должно быть ошибок 404 от Next.js

## Альтернатива

Если хотите использовать порт 3001, измените `backend/.env`:
```env
PORT=3001
```

И перезапустите backend.










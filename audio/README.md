# 📁 Папка для аудио файлов

Эта папка содержит аудио файлы для Playflon.

## Структура

```
audio/
├── focus/      # Треки для концентрации и работы
├── chill/      # Треки для расслабления
├── sleep/      # Треки для сна и медитации
└── ambient/    # Атмосферные треки
```

## Формат файлов

- Формат: MP3
- Имя файла: любое (например: `test.mp3`, `track_001.mp3`)
- Путь в Firestore: относительный (например: `focus/test.mp3`)

## Пример

Для трека с `filePath: "focus/test.mp3"` в Firestore:
- Файл должен быть: `audio/focus/test.mp3`

## Важно

- Файлы должны существовать физически на диске
- Backend проверяет существование файлов перед возвратом трека
- Если файл отсутствует, трек будет пропущен с предупреждением в логах









